# Base URL for the 24K Media API.
# - Local dev: leave EMPTY to use the Vite dev proxy (/api → localhost:4000).
# - Production (Vercel): set to your Render API URL, e.g.
#   https://24k-media-api.onrender.com
VITE_API_BASE=
