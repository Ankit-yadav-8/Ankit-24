# Base URL for the 24K Media API.
# - Local dev: leave EMPTY to use the Vite dev proxy (/api → localhost:4000).
# - Production (Vercel): set this in the Vercel dashboard (NOT this file),
#   to your Render API URL, e.g. https://two4k-media.onrender.com
VITE_API_BASE=
