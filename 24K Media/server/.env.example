# Port the 24K Media API listens on
PORT=4000

# Comma-separated list of allowed CORS origins (frontend URLs)
CORS_ORIGIN=http://localhost:5173

# Optional: forward new leads to an external CRM / webhook (e.g. Zapier, n8n)
LEAD_WEBHOOK_URL=
