{
  "name": "24k-media",
  "version": "1.0.0",
  "private": true,
  "description": "24K Media — We Build Internet Brands. Premium creator-growth agency website (React + Node + Express).",
  "scripts": {
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "dev": "concurrently -n \"API,WEB\" -c \"yellow,magenta\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
